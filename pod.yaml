kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  namespace: nginx
  labels:
    purpose: development

spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
